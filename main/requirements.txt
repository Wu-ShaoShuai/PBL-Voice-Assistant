# 核心框架
fastapi==0.115.12
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.6

# 日志与配置
loguru==0.7.3
PyYAML==6.0.2

# ASR (FunASR 本地模型)
funasr==1.2.7
torch==2.2.2
torchaudio==2.2.2
modelscope==1.32.0
numpy==1.26.4

# TTS (Edge TTS)
edge-tts==7.2.6

# LLM (OpenAI 兼容接口，用于智谱 AI)
openai==2.8.1

# 音频格式转换
pydub==0.25.1

# 辅助工具
jinja2==3.1.6          # 提示词模板渲染
cnlunar==0.2.0         # 农历转换
requests==2.32.5       # 上下文提供者（可选）
psutil==7.1.3          # 系统资源监控（可选）
silero-vad==6.1.0      # VAD 语音活动检测
onnxruntime==1.22.0    # VAD 模型推理